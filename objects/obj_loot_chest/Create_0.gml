depth = Depth.Container;
image_speed = 0;
/*show_message(instance_id);
show_message(string(layer_get_id(Collectable)));
layer_add_instance(layer_get_id(Collectable) , instance_id);
show_message(string(layer_has_instance(layer_get_id(Collectable), instance_id)));*/
depth_ = depth;
opened = false;
items = ds_list_create();
getLoot = true;