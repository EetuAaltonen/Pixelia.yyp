/*if (!is_undefined(followTarget)) {
	if (instance_exists(obj_player)) {
		show_message("Target changed!");
		if (followTarget == obj_player) {
			followTarget = obj_house_blacksmith;
		} else {
			followTarget = obj_player;
		}
	}
}